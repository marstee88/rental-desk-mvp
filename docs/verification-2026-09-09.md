# 正式管理版验收记录

## 自动检查

- 本机 Vitest：20 项通过。使用 PGlite 验证数据库权限、唯一约束和事务回滚；双连接测试在本机跳过。
- GitHub PostgreSQL 17：包括两条独立连接争抢同一间房的测试，通过。
- TypeScript 和 Vite 生产构建通过；Edge Function 的 Deno 类型检查通过。
- 接口模拟浏览器测试：3 项通过，覆盖 1280px、390px、新增房间、失败重试、已付清租客、明细、刷新和退出。

## 真实云端验收

使用两个独立、可丢弃的 QA 账号，不使用本人或员工的真实密码：

- 真实 Supabase 邮箱密码登录成功，管理员与员工角色正确。
- 匿名读取租客与合作分成返回 401；公开注册已关闭。
- 员工只能看到本人的成员记录，无法读取管理员合作分成。
- 新增房源/房间成功；相同提交编号重试不重复；重复房号被拒绝。
- 租客、入住关系和首期账单共同保存；重复入住被拒绝。
- 员工调用邀请函数返回 403；管理员通过身份检查，空邮箱返回预期 400，不发送邮件。
- 停用员工后，已有登录令牌无法读取房间或新增资料；启用后恢复。
- 两个独立浏览器会话实际登录：员工在 390px 页面新增房间、租客及首期账单；管理员在 1280px 页面能看到同一资料。
- 刷新后资料仍存在，租客明细正确，退出后页面清除租客资料；截图检查无横向页面溢出。

## 外部配置

- 已创建用户本人拥有的免费 Supabase 项目，9 张数据表启用 RLS。
- 已建立本人管理员成员权限并发出本人账号邀请。
- 已部署 `manage-staff`，使用函数内 `auth.getUser` 验证及有效管理员校验；匿名调用实测返回 401。
- 已配置正式网站地址和邀请/恢复回跳地址，关闭公开注册，最短密码设为 12。
- GitHub Pages 构建使用两项公开连接变量；没有管理员密钥进入仓库或网页。

## 尚待完成

- 自有 SMTP 发信服务尚未提供；普通员工邀请和密码恢复的实际收件验收尚未完成。默认 Supabase 发信仅用于项目团队地址测试。
- 本人的邮件链接设置密码流程，需正式页面发布后由本人验收。
- PR 需用户审阅并手动合并，尚未发布新版前端。

QA 资料及临时账号在验收后清理，正式数据库保留本人管理员，房间和租客从空列表开始。
